pipeline {
    agent {
        label 'docker'
    }
    stages {
        stage('.NET Version') {
            agent {
                docker { 
                    image 'dotnet:latest' 
                    label 'docker'
                }
            }
            steps {
                sh 'dotnet --version'
            }
        }
        stage('.NET Build') {
            agent {
                docker { 
                    image 'dotnet:latest' 
                    label 'docker'
                }
            }
            steps {
                sh 'dotnet build'
            }
        }
    }
}